# LLM Brand-Name Hallucination Benchmark — runtime dependencies
# Python 3.11+
#
# Kept deliberately small. NO pandas — orjson + csv stdlib is enough.
# httpx pins to a 0.27.x line that has stable async-client semantics.

httpx>=0.28.1,<1.0
pydantic>=2.13.4,<3.0
orjson>=3.11.9,<4.0
tqdm>=4.68.2,<5.0
python-dotenv>=1.2.2,<2.0

# Dev / test only — pytest is needed for `make test`
pytest>=9.1.0,<10.0
